---
title: 深度学习基础：模型是怎么训练的
date: '2026-09-16 18:00:00'
updated: '2026-09-16 18:00:00'
categories:
  - 技术
tags:
  - 深度学习
  - 学习笔记
---

<!-- Generated from Obsidian: Blogs/深度学习基础.md. Edit the source note, then run npm run blog:sync. -->

模型预测错了，可以用损失（Loss）衡量预测与真实值的差距。但 Loss 只是一个数，怎样从这个数知道每个参数该改多少？

![深度学习-机器学习整体流程.excalidraw](/img/blogs/deep-learning-basics/excalidraw-f1e08b3f.svg)

定义 Loss、确定候选函数集合、优化参数，再通过验证（Validation）和测试（Testing）检查效果，是机器学习的“3＋1”流程。

## 从一次错误的预测开始

假设只有一条样本：输入 $x=2$，真实值（标签）$y=5$。用 $\hat y$ 表示模型的预测值，模型是：

$$
\hat y=wx+b
$$

$w$ 是权重，决定直线的斜率；$b$ 是偏置，决定它上下平移多少。取初始参数 $w=1,b=0$，得到：

$$
\hat y=1\times2+0=2
$$

用当前参数算出预测值，叫作前向传播（forward propagation）。预测是 2，真实值是 5，可以用平方误差衡量这次预测：

$$
L=(\hat y-y)^2=(2-5)^2=9
$$

多条样本的误差如果直接相加，正负可能抵消。先平方再取平均，就得到均方误差（Mean Squared Error，MSE）：

$$
L=\frac{1}{N}\sum_{i=1}^{N}(\hat y_i-y_i)^2
$$

$N$ 是样本数，$\hat y_i,y_i$ 分别是第 $i$ 条样本的预测值和真实值。平方使较大的误差受到更大的惩罚，也让 MSE 对异常值更敏感。

把 $\hat y_i=wx_i+b$ 代进去：

$$
L(w,b)=\frac{1}{N}\sum_{i=1}^{N}(wx_i+b-y_i)^2
$$

数据固定以后，Loss 就成了 $w,b$ 的函数。改变参数会改变预测，也会改变 Loss；训练要找到能让它下降的改法。

## 从误差算到梯度

对开头的单个样本，预测和损失分别是：

$$
\hat y=wx+b,\qquad L=(\hat y-y)^2
$$

固定数据和 $b$，只让 $w$ 发生微小变化，Loss 对 $w$ 的变化率称为偏导数，记作 $\partial L/\partial w$。

$w$ 先影响预测值 $\hat y$，再通过 $\hat y$ 影响 Loss。链式法则将这条路径上两步的变化率相乘：

$$
\frac{\partial L}{\partial w}
=\frac{\partial L}{\partial\hat y}
\frac{\partial\hat y}{\partial w}
=2(\hat y-y)x
$$

$b$ 对预测值的导数是 1，因此：

$$
\frac{\partial L}{\partial b}
=2(\hat y-y)
$$

代入 $x=2,y=5,w=1,b=0$：

$$
\frac{\partial L}{\partial w}=2(2-5)\times2=-12,\qquad
\frac{\partial L}{\partial b}=2(2-5)=-6
$$

例如，固定 $b$，把 $w$ 增加很小的 $\Delta w$，Loss 的变化约为 $-12\Delta w$。负号说明此处增大 $w$ 能减小 Loss；$b$ 的偏导也是负数，所以此处增大 $b$ 同样能让 Loss 下降。

所有参数的偏导数组成梯度。记 $\theta=(w,b)$，此时：

$$
\nabla_\theta L=
\begin{bmatrix}
\partial L/\partial w\\
\partial L/\partial b
\end{bmatrix}
=
\begin{bmatrix}
-12\\
-6
\end{bmatrix}
$$

在通常的欧氏距离下，梯度指向当前位置局部上升最快的方向，负梯度指向局部下降最快的方向。$\partial L/\partial w$ 只是其中一个分量。

多条样本使用平均 Loss，各个样本的梯度也相应取平均：

$$
\frac{\partial L}{\partial w}
=\frac{2}{N}\sum_{i=1}^{N}(wx_i+b-y_i)x_i,\qquad
\frac{\partial L}{\partial b}
=\frac{2}{N}\sum_{i=1}^{N}(wx_i+b-y_i)
$$

把乘法、加法、平方等运算作为节点，按数据依赖关系连接起来，就得到计算图。上面的计算依次经过 $wx$、$wx+b$、减去 $y$ 和平方，梯度可以沿这些依赖反向计算。

反向传播（Backpropagation）从 Loss 出发，沿计算图反向应用链式法则，算出各个参数的梯度。有分支时，各条路径的梯度贡献需要相加。这个过程完成后，参数还没变；优化器再按选定的规则，利用梯度更新参数。下面使用的更新规则是梯度下降。

### 学习率决定迈多大一步

梯度下降用下面的式子更新参数：

$$
\theta_{t+1}=\theta_t-\eta\nabla_\theta L(\theta_t)
$$

$t$ 是更新次数，$\eta$ 是学习率（learning rate）。取 $\eta=0.1$：

$$
w_{\mathrm{new}}=1-0.1\times(-12)=2.2
$$

$$
b_{\mathrm{new}}=0-0.1\times(-6)=0.6
$$

两个梯度都用更新前的同一组参数计算。再用新参数预测：

$$
\hat y_{\mathrm{new}}=2.2\times2+0.6=5,\qquad
L_{\mathrm{new}}=0
$$

这次恰好一步把 Loss 降到了 0，是样本、初始参数和学习率共同作用的结果。只有一个样本，却有两个参数，所有满足 $2w+b=5$ 的参数组合都能拟合它。这些直线在其他输入上的预测可以完全不同。

如果从相同的初始参数出发，把学习率改成 $\eta=0.3$，更新后就会得到：

$$
w_{\mathrm{new}}=4.6,\qquad b_{\mathrm{new}}=1.8,\qquad
\hat y_{\mathrm{new}}=11,\qquad L_{\mathrm{new}}=36
$$

更新方向没变，Loss 却从 9 增加到了 36。梯度给的是局部信息，步子太大就可能越过低点。学习率过小则可能让训练很慢。

![Drawing 2026-09-15 22.21.57.excalidraw 2](/img/blogs/deep-learning-basics/drawing-2026-09-15-22-21-57-excalidraw-2-996835ac.svg)

计算 Loss 还需要真实标签，图中没有单独画出这条输入。

## 从一个样本到一批数据

训练集由成对的输入和标签 $(x_i,y_i)$ 组成。每次更新可以使用整份数据，也可以只取其中一部分。

数据多起来以后，整个训练集一起计算可能超过显存容量；一次只处理一个样本，又可能用不满并行计算资源。因此通常把数据分成小批次（mini-batch，简称 batch），用当前 batch 的平均 Loss 求梯度，再更新参数。

| 更新方式 | 每次使用的数据 |
| --- | --- |
| 全批量梯度下降 | 整个训练集 |
| 随机梯度下降（Stochastic Gradient Descent，SGD，狭义） | 随机抽取的一个样本 |
| 小批量随机梯度下降 | 一个 mini-batch |

实践中说“用 SGD 训练”，经常也包括小批量的情况。

batch 指一次送入模型的一批数据；batch size（批量大小）是每批的样本数；optimizer step（优化器更新步）是一次参数更新；epoch（训练轮次）是训练集完整遍历一遍。例如有 1000 条样本，batch size 为 100，每个 batch 更新一次，那么一个 epoch 就有 10 次更新，20 个 epoch 共更新 200 次。

样本数不能整除 batch size 时，最后一批通常会小一些，也可以配置为丢弃。使用梯度累积时，则会处理多个 batch 才更新一次参数，所以日志中的 step（步骤）或 iteration（迭代）需要结合代码判断。

对于可以独立采样的数据，每个 epoch 开始时通常会随机打乱样本顺序（shuffle），让不同轮次的 batch 组成发生变化，减少固定排序对更新过程的影响。打乱的是成对的 $(x_i,y_i)$；时间序列或有状态训练要另外考虑时序，不能直接照搬。

不同 batch 的难度和梯度不同，Loss 曲线会有波动。相比某一步是否下降，一段时间内的趋势更有参考价值。

$w,b$ 这类参数由训练学习；学习率、batch size、训练轮数等由人或外部搜索过程设置，称为超参数。

## 直线不够用时，为什么需要非线性激活函数

不管更新多少次 $w,b$，$\hat y=wx+b$ 始终是一条直线。写下这个模型时，其实已经限定了候选函数集合：

$$
\mathcal F=\{x\mapsto wx+b\mid w,b\in\mathbb R\}
$$

每组参数对应集合中的一个函数。训练是在这个集合里寻找合适的直线；如果需要拟合的关系明显弯曲，最好的直线也可能留下很大的误差。换优化器不会改变这个限制。

输入和输出都有多个数值时，可以把标量形式的 $wx+b$ 写成 $Wx+b$：$x$ 是输入向量，$W$ 是权重矩阵，$b$ 是偏置向量。$W$ 的每一行对输入做一次加权求和，再加上对应的偏置，得到一个输出分量。这样的计算在神经网络中称为线性层。

把一层的输出送入下一层，如果两层都只计算 $Wx+b$，就能合并成一层。下标和偏置上的括号表示层号：

$$
W_2(W_1x+b^{(1)})+b^{(2)}
=(W_2W_1)x+(W_2b^{(1)}+b^{(2)})
$$

严格说，$Wx$ 是线性变换，加上偏置后的 $Wx+b$ 称为仿射变换。上式说明，直接叠加这样的层，仍然表示不了一般的非线性关系。

激活函数接在线性层之后，对计算结果再做一次变换。使用非线性的激活函数，可以让多层网络表示更复杂的关系。修正线性单元（Rectified Linear Unit，ReLU）是一种常用选择，它在 0 处加入了一个折点：

$$
\operatorname{ReLU}(z)=\max(0,z)
$$

输入小于 0 时输出 0，大于 0 时保留原值。把几个位置不同的 ReLU 加权相加，就能改变不同区间的斜率。例如：

$$
f(x)=\operatorname{ReLU}(x)-2\operatorname{ReLU}(x-1)+\operatorname{ReLU}(x-2)
$$

![深度学习-ReLU叠加曲线.excalidraw](/img/blogs/deep-learning-basics/relu-excalidraw-1e7dcbdb.svg)

三个折点分别在 $0,1,2$。把各项代进去：

$$
f(x)=
\begin{cases}
0,&x<0\\
x,&0\le x<1\\
2-x,&1\le x<2\\
0,&x\ge2
\end{cases}
$$

在 $1\le x<2$ 时，前两项已经激活，第三项仍是 0，所以 $f(x)=x-2(x-1)=2-x$。到了 $x\ge2$，三项恰好抵消。总斜率从 0 变成 1，再变成 −1，最后回到 0，拼出了一段先上升、再下降的折线，最高点是 $(1,1)$。

这里的参数是手动选好的，用来说明函数怎样构成，不是训练结果。四个小图的纵轴范围不同，应按公式比较斜率，不能直接比较线段的倾斜角度。

### 把这些 ReLU 写成一个网络

上面的三个 ReLU，对应输入权重 $w_i=1$、偏置 $(b_1,b_2,b_3)=(0,-1,-2)$、输出权重 $(c_1,c_2,c_3)=(1,-2,1)$，输出偏置为 $b_0=0$。把这些固定值换成可训练参数：

$$
h_i=\operatorname{ReLU}(w_ix+b_i),\qquad
\hat y=b_0+\sum_{i=1}^{H}c_i h_i
$$

这里一次“加权求和、加偏置、经过激活函数”的计算构成一个神经元，$h_i$ 是第 $i$ 个神经元的输出。这些神经元位于输入和最终输出之间，组成隐藏层；$H$ 是这一层的神经元数量。

对于 $w_i\ne0$，折点在 $x=-b_i/w_i$；$w_i$ 的正负决定哪一侧被激活，$c_iw_i$ 决定激活一侧对总斜率的贡献。$b_0$ 让整个函数上下平移。

特征是描述一个样本的输入属性，例如课程的投影片页数。把多个数值特征放在一起，就得到输入向量。如果输入有两个特征 $x_1,x_2$，每个隐藏神经元先计算 $w_{i,1}x_1+w_{i,2}x_2+b_i$，再经过 ReLU：

![深度学习-神经网络结构.excalidraw](/img/blogs/deep-learning-basics/excalidraw-92399e10.svg)

每个隐藏神经元都接收两个输入，但有各自的权重和偏置。输出层将三个隐藏值加权求和，再加上输出偏置。把同一层的计算合在一起，可以写成：

$$
z=W_1x+b^{(1)},\qquad h=\operatorname{ReLU}(z)
$$

$$
\hat y=W_2h+b^{(2)}
$$

ReLU 对向量的每个分量分别计算。若输入有 $d$ 个特征，隐藏层有 $m$ 个神经元，$W_1$ 的形状就是 $m\times d$，隐藏层偏置和 $h$ 都有 $m$ 个分量。

图中，隐藏层偏置向量 $b^{(1)}=(b_1,b_2,b_3)^\mathsf{T}$，输出偏置 $b^{(2)}=b_0$。上标 $\mathsf{T}$ 表示转置，这里将横向排列的数写成列向量。

多个神经元组合后，可以表示更复杂的分段线性关系，近似很多非线性函数。继续把隐藏层的输出送进下一层，就得到多层神经网络。深度学习通常用这样的网络学习中间表示和参数；这些权重和偏置仍然可以通过反向传播求梯度，再由优化器更新。

## 模型能表示，不等于训练能找到

机器学习基础课件《BasicML》（ML 为 Machine Learning，即机器学习）用投影片页数预测课程时长：线性模型的训练 Loss 约为 71，换成有 100 个隐藏神经元的网络后，训练 Loss 反而约为 80；调整超参数后才降到约 41。

把全部参数记作 $\theta$，训练目标通常写成：

$$
\theta^*\in\operatorname*{arg\,min}_\theta L_{\mathrm{train}}(\theta)
$$

$\arg\min$ 表示取得最小值的参数集合；可能有多组最优参数，所以这里用 $\in$。全局最小值（Global Minimum）是在全部允许的参数中比较得到的最低 Loss；局部极小值（Local Minimum）只要求在某组参数附近，没有更低的 Loss。上式写的是全局优化目标，实际训练未必能达到。

如果大模型的候选集合包含小模型的全部函数，且使用相同训练数据和损失，理论上有：

$$
\inf_{f\in\mathcal F_{\text{large}}}L_{\mathrm{train}}(f)
\le
\inf_{f\in\mathcal F_{\text{small}}}L_{\mathrm{train}}(f)
$$

$\inf$ 表示能达到或无限接近的下界。原来的函数都还在，最好的可能结果就不会更差。但实际训练只得到了某组参数，未必到达这个下界。大模型训练得更差时，需要检查优化过程、实现和比较设置，不能直接归因于表达能力不足。

前面用 $wx+b$ 预测数值的模型属于线性回归。它配合 MSE 时，Loss 关于参数是凸函数：任取两组参数，将它们的损失点用线段相连，沿两组参数之间直线计算出的 Loss 都不高于这条线段。凸函数的局部极小值也是全局最小值。最优参数可能不唯一，例如输入特征存在冗余；学习率不合适时，梯度下降也可能不收敛。

神经网络的 Loss 通常是非凸的。参数初始化就是选择训练开始时的参数值，不同初始值可能让优化走出不同的路径。图中还标出了 Saddle Point（鞍点），其多方向的含义在图后解释：

![Drawing 2026-09-16 14.11.12.excalidraw](/img/blogs/deep-learning-basics/drawing-2026-09-16-14-11-12-excalidraw-e2c5f1e8.svg)

这是一张非凸损失的简化示意图，不是前面线性回归的 MSE 曲线。真实网络有很多参数，Loss 定义在高维参数空间里。

梯度接近 0 可能出现在局部极小值、局部极大值、鞍点或较平坦的区域。鞍点附近，沿某些方向向上，沿另一些方向却能继续向下。深层网络还可能遇到梯度消失或梯度爆炸：梯度在逐层反向传播时变得过小，可能使前面层的参数几乎不更新；变得过大，则可能导致更新不稳定。不同方向上的变化尺度也可能差异很大。初始化、数据顺序、学习率和优化器都会影响训练路径。

## 训练 Loss 很低之后，还要看什么

假设造一个“记答案”的函数：遇到训练样本就返回记住的标签，其他输入一律返回 0。它在训练集上的 Loss 可以是 0，但换一批数据就可能错得很严重。

这就是泛化问题：模型对没见过的数据表现怎样。课件中的课程时长模型增加特征后，训练 Loss 约为 22，验证 Loss 却约为 1307。训练拟合得更好，没有带来相应的验证效果。

| 数据划分 | 用途 |
| --- | --- |
| 训练集 | 计算训练 Loss，通过梯度更新参数 |
| 验证集 | 比较模型、选择超参数、判断何时停止训练 |
| 测试集 | 在模型和设置确定后，评估最终效果 |

过拟合指模型过度适应了训练数据中的细节或噪声，在新数据上的表现较差。例如训练 Loss 持续下降，验证 Loss 却开始回升，就可能已经过拟合：

![深度学习-训练与验证Loss.excalidraw](/img/blogs/deep-learning-basics/loss-excalidraw-a343f6dc.svg)

图中只画趋势，不是实验记录。早停是根据验证表现决定何时结束训练的方法，通常会容忍若干轮没有改善，再停止训练，恢复验证表现较好的检查点。检查点保存了某次训练时的模型参数，也可以包含优化器等训练状态。

欠拟合指模型尚未充分学到数据中的规律，连训练数据也拟合不好。训练和验证都差，可能是欠拟合，也可能是数据、训练设置或实现有问题；训练很好而验证明显较差，除了过拟合，也要检查两份数据是否存在分布差异。单凭一条验证曲线，不能确定全部原因。

课程时长的例子最初用 2021 年的课程数据预测 2025 年的课程，后来改用了更接近的 2024 年数据。训练数据和目标场景不合适时，在旧数据上继续压低 MSE，也未必能改善目标场景下的预测。

出现过拟合时，除了早停，还可以检查数据的质量和数量。数据增强会对已有样本做保持标签含义的变换，例如对适合翻转的图片做水平翻转；调整模型容量，则是改变模型能表示的函数范围，例如减少隐藏神经元。

正则化通过额外约束或训练设置限制过拟合，例如在损失中加入权重平方和的惩罚。Dropout（随机失活）也是一种正则化方法：训练时随机将部分神经元的输出置零，减少网络对特定中间输出的依赖。

评估数据也要与训练隔离。例如，常见的特征标准化会先减去均值，再除以标准差，让不同特征的数值尺度更接近。均值和标准差应只从训练集估计，再用于处理验证集和测试集；标准差为 0 的特征需要单独处理。

### 验证集也会被反复调参用熟

验证集不直接参与梯度更新，但每次根据验证结果修改模型、特征或超参数，都在利用它选择方案。尝试得足够多，可能选出一个恰好适合这份验证集、对新数据却没有同样优势的模型。

因此，测试集要留到模型和设置确定后再用。如果看完测试结果又继续调参，这份测试集也参与了模型选择，后续就需要新的独立数据评估。

## 用代码核对一次更新

PyTorch 是用于张量计算和神经网络训练的框架，使用 Python（一种通用编程语言）编写程序，能记录运算并自动计算梯度。这里的张量（tensor）可以理解为按维度组织的数值数组，单个数、向量和矩阵分别对应零维、一维和二维张量。代码中的 `torch` 是框架的导入名，`nn` 取自 neural network（神经网络），是提供网络层和损失函数的模块；`nn.Linear` 是线性层，`nn.MSELoss` 是前面介绍的均方误差损失。

取 $x=2,y=5,w=1,b=0,\eta=0.1$，用 PyTorch 计算同一次更新：

~~~python
import torch
from torch import nn

# 形状是 [样本数, 特征数]，这里各为 1。
x = torch.tensor([[2.0]])
y = torch.tensor([[5.0]])

model = nn.Linear(1, 1)  # 输入和输出各有 1 个特征：y_hat = wx + b
with torch.no_grad():     # 手动设定初始参数，不记录这段操作的梯度
    model.weight.fill_(1.0)
    model.bias.zero_()

loss_fn = nn.MSELoss()
optimizer = torch.optim.SGD(model.parameters(), lr=0.1)

model.train()
optimizer.zero_grad()       # 清除旧梯度
prediction = model(x)      # 前向传播
loss = loss_fn(prediction, y)
loss.backward()            # 反向传播，计算梯度

print(f"更新前 Loss: {loss.item():.4f}")
print(f"w 的梯度: {model.weight.grad.item():.4f}")
print(f"b 的梯度: {model.bias.grad.item():.4f}")

optimizer.step()           # 根据梯度更新参数

model.eval()
with torch.no_grad():
    new_prediction = model(x)
    new_loss = loss_fn(new_prediction, y)

print(f"更新后 w: {model.weight.item():.4f}")
print(f"更新后 b: {model.bias.item():.4f}")
print(f"更新后 Loss: {new_loss.item():.4f}")
~~~

按前面的手算，应得到以下输出；浮点数内部可能有微小误差：

~~~text
更新前 Loss: 9.0000
w 的梯度: -12.0000
b 的梯度: -6.0000
更新后 w: 2.2000
更新后 b: 0.6000
更新后 Loss: 0.0000
~~~

PyTorch 默认会累积梯度，所以这里在反向传播前调用 `optimizer.zero_grad()` 清除旧梯度。有意做梯度累积时，清除梯度和更新参数的时机需要另行安排。

更新参数以后，原来的 `loss` 仍然是更新前算出的结果。想知道新参数下的 Loss，需要重新预测、重新计算。

`model.train()` 和 `model.eval()` 分别设置训练模式和评估模式。例如，Dropout 在训练时随机置零，在评估时关闭这一随机操作。本例只有线性层，切换模式不会改变预测计算。`model.eval()` 也不会关闭求导；评估时通常还会用 `torch.no_grad()` 关闭梯度记录。

这些应用程序编程接口（Application Programming Interface，API）的分工可对照 [PyTorch 官方训练教程](https://docs.pytorch.org/tutorials/beginner/basics/optimization_tutorial)。


## 分类：从类别分数到交叉熵

前面的回归模型预测一个数值。换成图片分类，假设每张图片只属于“猫、狗、兔”中的一类，模型需要给出三个类别各自的分数，再根据真实标签计算 Loss。

### 标签和模型输出怎样对应

固定类别顺序为“猫、狗、兔”，可以用编号 $0,1,2$ 保存标签。编号只用于区分类别，本身没有大小或距离的含义。计算损失时，也可以把标签写成独热编码（one-hot）：真实类别的位置为 1，其余位置为 0。

| 类别 | 编号 | 独热标签 |
| --- | --- | --- |
| 猫 | 0 | $(1,0,0)$ |
| 狗 | 1 | $(0,1,0)$ |
| 兔 | 2 | $(0,0,1)$ |

假设当前图片是一只狗，标签向量就是 $y=(0,1,0)$。这里 $y$ 的每个分量对应一个类别；前面的回归例子中，$y$ 是一个标量。

网络最后一层可以用线性变换输出三个分数：

$$
z=W_{\mathrm{out}}h+b_{\mathrm{out}}
$$

$h$ 是隐藏层输出。若 $h$ 有 $m$ 个分量，这里的 $W_{\mathrm{out}}$ 就是 $3\times m$ 的矩阵。这三个未经归一化的分数称为 logits，可以为任意实数，也不要求加起来等于 1。

假设这次输出：

$$
z=(1,2,0)
$$

“狗”的分数最高。只需要选出预测类别时，直接取 logits 最大值的位置即可；要计算类别概率，还需要 softmax（归一化指数函数，将一组分数转成和为 1 的概率）。

### softmax 把分数转成概率

对于 $C$ 个类别，softmax 定义为：

$$
p_c=\frac{e^{z_c}}{\sum_{k=1}^{C}e^{z_k}}
$$

每一项都大于 0，所有项的和为 1。代入这次的 logits：

$$
p=\frac{(e^1,e^2,e^0)}{e^1+e^2+e^0}
\approx(0.2447,\ 0.6652,\ 0.0900)
$$

模型分配给“狗”的概率约为 $66.52\%$，所以预测类别为“狗”。这个数表示模型当前给出的概率估计，是否与实际正确率相符，还需要用数据检验。

softmax 由各类别分数的相对差异决定。给所有 logits 加上相同常数，概率保持不变。计算时常先减去最大值：

$$
p_c=\frac{e^{z_c-a}}{\sum_{k=1}^{C}e^{z_k-a}},
\qquad a=\max_k z_k
$$

本例中的 $(1,2,0)$ 因而可以换成 $(-1,0,-2)$，结果相同。这样可以避免对很大的正数求指数时发生溢出。

### 预测已经正确，为什么 Loss 还大于 0

对独热标签 $y$ 和预测概率 $p$，单个样本的交叉熵（cross-entropy）为：

$$
\ell=-\sum_{c=1}^{C}y_c\log p_c
$$

这里使用自然对数。只有真实类别的位置满足 $y_c=1$，其余项都为 0。记真实类别的位置为 $t$，公式就简化为：

$$
\ell=-\log p_t
$$

这张图片的真实类别是“狗”，因此：

$$
\ell=-\log 0.6652409558\approx0.4076
$$

虽然预测类别已经正确，模型仍给其他类别分配了概率。真实类别的概率越接近 1，交叉熵越接近 0；概率越接近 0，损失越大。例如把“狗”的概率从当前值提高到 $0.9$，损失就会降到约 $0.1054$。

准确率是预测正确的样本数占总样本数的比例。交叉熵还会随真实类别的概率变化，因此分类准确率不变时，Loss 仍然可能下降。

对一个含 $N$ 个样本的 batch，取平均损失：

$$
L=-\frac{1}{N}\sum_{i=1}^{N}\sum_{c=1}^{C}y_{i,c}\log p_{i,c}
$$

softmax 和交叉熵连起来，对单个样本的 logits 求导，结果很简洁：

$$
\frac{\partial\ell}{\partial z_c}=p_c-y_c
$$

代入本例：

$$
\nabla_z\ell
\approx(0.2447,\ -0.3348,\ 0.0900)
$$

如果直接沿负梯度调整这三个分数，“狗”的分数会增大，另外两个会减小。实际网络中，logits 由权重和偏置计算得到，梯度还要沿计算图继续传播到这些参数，再由优化器更新。

### 在 PyTorch 中计算

`nn.CrossEntropyLoss`（交叉熵损失接口）接收原始 logits。下面使用类别编号作为标签，`logits` 的形状为 `[样本数, 类别数]`，`target`（目标标签）的形状为 `[样本数]`，类型为表示 64 位整数的 `torch.long`。这种写法省去了显式构造独热向量的步骤。

~~~python
import torch
from torch import nn

logits = torch.tensor([[1.0, 2.0, 0.0]], requires_grad=True)  # 需要计算对 logits 的梯度
target = torch.tensor([1], dtype=torch.long)  # 狗的类别编号为 1

loss = nn.CrossEntropyLoss()(logits, target)
loss.backward()

# softmax 用来查看概率；计算上面的 Loss 时直接传入 logits。
probabilities = logits.detach().softmax(dim=1)  # 第 1 维是类别维，第 0 维是样本维
predicted_class = logits.detach().argmax(dim=1)  # 取每行最大值的类别编号

print(probabilities)
print(predicted_class)
print(f"{loss.item():.4f}")
print(logits.grad)
~~~

按公式计算，预期结果为：

~~~text
tensor([[0.2447, 0.6652, 0.0900]])
tensor([1])
0.4076
tensor([[ 0.2447, -0.3348,  0.0900]])
~~~

`requires_grad=True` 让 PyTorch 记录与该张量有关的运算，以便求梯度；这里手动创建了 logits，所以需要显式设置。`detach()` 返回与原计算图分离的张量，数值不变，之后查看概率的运算无需继续记录梯度。相关行为见 [PyTorch 自动求导教程](https://docs.pytorch.org/tutorials/beginner/basics/autogradqs_tutorial.html)。

不要先对 logits 做 softmax 再传入 `CrossEntropyLoss`。它内部包含等价于 `LogSoftmax` 加负对数似然的计算：`LogSoftmax` 得到各类别的对数概率 $\log p_c$；对类别编号标签，负对数似然取出真实类别的那一项并取负号，也就是前面的 $-\log p_t$。实现会处理数值稳定性，接口说明见 [PyTorch CrossEntropyLoss 文档](https://docs.pytorch.org/docs/stable/generated/torch.nn.CrossEntropyLoss.html)。

### 一张图片有多个标签时

前面每个样本只有一个正确类别，属于单标签分类。如果图片中可以同时出现猫和狗，就属于多标签分类。仍按“猫、狗、兔”的顺序，标签可以写成 $y=(1,1,0)$，每个位置分别表示该类别是否出现。

模型仍输出三个 logits，但各类别分别用 sigmoid（S 形函数，这里指将实数映射到 0 与 1 之间的逻辑函数）转成概率：

$$
p_c=\sigma(z_c)=\frac{1}{1+e^{-z_c}}
$$

sigmoid 将单个实数映射到 0 和 1 之间。三个概率分别计算，不要求和为 1，因此猫和狗的概率可以同时很高。预测时可对每一类设置阈值，例如将概率超过 $0.5$ 的类别标记为出现；阈值也可以根据验证结果调整。

对每个类别，使用二元交叉熵（Binary Cross-Entropy，BCE）：

$$
\ell_c=-\left[y_c\log p_c+(1-y_c)\log(1-p_c)\right]
$$

当 $y_c=1$ 时，损失为 $-\log p_c$；当 $y_c=0$ 时，损失为 $-\log(1-p_c)$。前者要求出现的类别概率高，后者要求未出现的类别概率低。各类别的损失可以再取平均，得到一个样本的损失。

PyTorch 的 `nn.BCEWithLogitsLoss` 将 sigmoid 和二元交叉熵合在一起，直接接收原始 logits。多标签情况下，logits 和浮点标签的形状都为 `[样本数, 类别数]`；默认对所有样本和类别的损失取平均。不要在传入前再做 sigmoid。参见 [BCEWithLogitsLoss 文档](https://docs.pytorch.org/docs/stable/generated/torch.nn.BCEWithLogitsLoss.html)。

## 自测问题

### 梯度和参数更新

1. 对 $\hat y=wx+b$，取 $x=2,y=5,w=1,b=0$，使用平方误差。手算预测值、Loss，以及 Loss 对 $w,b$ 的偏导数。为什么两个偏导数都是负数？

2. 从第 1 题的初始参数出发，分别用学习率 $0.1$ 和 $0.3$ 更新一次参数，再计算各自的新 Loss。沿负梯度更新，为什么仍可能让 Loss 增大？

3. 一个样本上的 Loss 降到了 0，能否确定唯一的 $w,b$？写出两组都能拟合 $(2,5)$ 的参数，再比较它们在 $x=3$ 时的预测。

4. 有 1000 条训练样本，batch size 为 100，每个 batch 更新一次参数。训练 20 个 epoch，共更新多少次？如果每处理 2 个 batch 才更新一次，更新次数怎样变化？

5. 在文中的 PyTorch 代码里，`loss.backward()` 和 `optimizer.step()` 分别改变什么？如果每个 batch 都打算独立更新，却一直没有清除旧梯度，会发生什么？参数更新后，直接打印原来的 `loss`，得到的是哪次计算的结果？

### 表达能力、优化和泛化

6. 将 $W_2(W_1x+b^{(1)})+b^{(2)}$ 展开，说明没有激活函数的两层网络为什么仍是仿射变换。对文中的三个 ReLU 叠加算例，分别计算 $x=0.5,1.5,3$ 时的输出，指出各处有哪些项被激活。

7. 两个模型使用相同训练数据和损失函数，大模型能表示小模型的全部函数。为什么大模型实际训练得到的 Loss 仍可能更高？如果某处梯度接近 0，能否据此认定已经找到最优参数？

8. 训练 Loss 持续下降，验证 Loss 却开始回升，可能有哪些原因？如果使用早停，应根据什么选择保留的参数？为什么不一定保留最后一次更新的结果？

9. 验证集没有参与反向传播，为什么反复根据验证结果调参，仍可能让评估偏乐观？如果看过测试结果以后继续改模型，这份测试集还能承担原来的评估作用吗？

### 分类和交叉熵

10. 类别顺序为“猫、狗、兔”，真实类别是“狗”。分别写出类别编号和独热标签。如果把顺序改为“狗、猫、兔”，标签和模型输出各需要怎样对应？

11. 对 logits $z=(1,2,0)$ 手算 softmax。将 logits 改成 $(101,102,100)$，概率会变化吗？改成 $(2,4,0)$ 呢？解释给所有分数加同一个数与乘同一个正数的区别。

12. 真实类别仍为“狗”，模型给出的概率分别为 $(0.2,0.7,0.1)$ 和 $(0.1,0.8,0.1)$。两次预测是否正确？分别计算交叉熵，并解释为什么准确率相同，Loss 却不同。

13. 对 $p=(0.2,0.7,0.1)$、$y=(0,1,0)$，计算交叉熵对三个 logits 的梯度。每个分量的正负意味着什么？这些梯度还需要经过哪些计算，才能用于更新输出层权重？

14. 一个 batch 有 32 张图片、3 个互斥类别，使用类别编号作为标签。传给 `nn.CrossEntropyLoss` 的 logits 和 target 应各是什么形状？target 应使用什么数据类型？如果先做 softmax 再传入损失函数，哪里出了问题？如果一张图片可以同时有“猫”和“狗”两个标签，输出与损失应怎样调整？

参考课件：《BasicML》。

